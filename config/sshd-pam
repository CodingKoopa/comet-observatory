# SSH Daemon PAM policy. This only defines authentication through the Google Authenticator PAM
# module, other authentication is specified in the SSH daemon configuration.

# Require a secure TTY, if logging into root.
auth      required  pam_securetty.so
# Require Google Authenicator 2FA authentication.
auth      required  pam_google_authenticator.so
# Include account verification from normal system remote login.
account   include   system-remote-login
# Include password changing from normal system remote login.
password  include   system-remote-login
# Include user session management from normal system remote login.
session   include   system-remote-login
